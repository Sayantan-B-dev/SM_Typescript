an user interface.. an admin interface..but 
we can use extends...
the use of extends..and the working under the hood

interface A{
    name:string
}

interface B extends A{
...
}

now if we use B in function..


what if two interface has same name.. they merges

under the hood with example
