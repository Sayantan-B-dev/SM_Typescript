making custom type..
type n=number

why its useless

instedd let a:string|number|null

we use type value=string|number|nul
let a:value;

in function too in object....we might need strting or null or two different objects...we can create a custom type fr that...
